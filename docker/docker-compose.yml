version: '3'

services:
  shiny:
    container_name: base_app
    image: baseApp
    restart: unless-stopped
    ports:
    - "3838:3838"
    networks:
    - shiny_app_net

networks:
  shiny_app_net:
